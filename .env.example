# 📦 MarkIssue Environment Configuration
# Automatically loaded by docker-compose and configurable for the host

# The directory where all Markdown issues and JSON dictionaries are stored
MARKISSUE_DATA_DIR=LocalStorage

# (Optional) Port mapping if you need to change it on the host side for Docker
# APP_PORT=8505
